import { PayToQRForm } from "@/components/layout/pay-to-qr";
import React from "react";

const Pay = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <PayToQRForm />
    </div>
  );
};

export default Pay;
